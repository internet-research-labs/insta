(ns insta.gram
  )

(defn auth [req]
  "
  authenticate with
  @param req http-server-request request object
  @return 1
  "
  1)

(defn subscribe-tag [tag]
  "Subscribe to a tag"
  -1)

(defn unsubscribe-tag [tag]
  "Unsubscribe to a tag"
  -1)

(defn handshake [req]
  "Handshake"
  2)

(defn receive [req]
  3)
